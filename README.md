# integral_image
```
Allowed options:
  -h [ --help ]             produce help message
  -i [ --image ] arg        path to image (required)
  -t [ --threads ] arg (=0) number of threads, 0 - detect automatically
```
